name: CI

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: 12d2bccb5c93686c3053838660b6605b8d7510af

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install dev requirements
        run: pip install -r requirements-dev.txt

      - name: Install package
        run: pip install -e .

      - name: Check repository boundary
        run: python scripts/check_conos_repo_layout.py

      - name: Run public smoke test
        run: pytest -q tests/test_public_repo_smoke.py

      - name: Verify research packet evidence
        run: python research/ai_generated_agi_architectures/verify_packet.py

      - name: Execute isolated crash experiment
        run: python research/ai_generated_agi_architectures/recovery_experiment.py
